/**
 * 请求装饰器
 * 处理所有 get post 请求的前后
 * 如 loading 动画等
 * */
// import AL from './autoloading'

export default {
  before : (al, ae) => {
    return (req, res, next) => {
    }
  },

  after: (al, ae) => {
    return (req, res, next) => {
    }
  }
}